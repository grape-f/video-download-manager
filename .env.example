# ============================================================
# 在线视频下载管理器 - 环境变量配置
# 复制本文件为 .env 后按需修改
# ============================================================

# 后端服务端口
PORT=8787
# 监听地址
HOST=0.0.0.0

# 数据目录（SQLite 数据库存放位置，相对于项目根目录）
DATA_DIR=./data
# 下载目录（视频文件保存位置，相对于项目根目录）
DOWNLOAD_DIR=./downloads

# 最大并发下载任务数
MAX_CONCURRENT=3
# 默认下载质量（best / 2160p / 1440p / 1080p / 720p / 480p / 360p）
DEFAULT_QUALITY=best
# 默认文件格式（mp4 / webm / mkv）
DEFAULT_FORMAT=mp4

# 最大下载速度（如 8M / 2M，留空表示不限速）
MAX_SPEED=
# 请求超时时间（毫秒）
REQUEST_TIMEOUT_MS=120000
# 自动重试次数
AUTO_RETRIES=3

# yt-dlp 可执行文件路径（默认使用 PATH 中的 yt-dlp）
YTDLP_PATH=yt-dlp
# ffmpeg 目录（包含 ffmpeg/ffmpeg.exe，用于高清音视频合并）
FFMPEG_PATH=./bin/ffmpeg

# 是否启用模拟下载源（sim:// 协议，用于离线测试；生产环境建议 false）
ENABLE_SIMULATE=true
