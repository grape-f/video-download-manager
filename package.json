{
  "name": "video-download-manager",
  "version": "1.2.0",
  "private": true,
  "description": "在线视频下载管理器 - 统一管理你的在线视频下载任务",
  "workspaces": [
    "server",
    "web"
  ],
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev -w server\" \"npm run dev -w web\"",
    "dev:server": "npm run dev -w server",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w server && npm run build -w web",
    "start": "npm run start -w server",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w web",
    "clean": "node -e \"const fs=require('fs');['dist','server/dist','web/dist'].forEach(d=>fs.rmSync(d,{recursive:true,force:true}))\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
